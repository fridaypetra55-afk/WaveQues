# Soroban Contracts

Contains four Soroban smart contract scaffolds:

- `learning` — register learners, track XP and streaks
- `rewards` — distribute XLM and bonuses
- `achievement` — mint badges and certificates
- `leaderboard` — rank users by XP

Each contract is a small Rust/Soroban scaffold to be expanded.
